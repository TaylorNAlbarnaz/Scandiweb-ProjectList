import { Header, ProductList } from '../Components';

function MainPage() {
  return (
    <div className='container'>
      <Header/>
      <ProductList/>
    </div>
  );
}

export default MainPage;
