import { Header, ProductList, DeleteModal} from '../Components';

function MainPage() {
  return (
    <div className='container'>
      <Header/>
      <ProductList/>
    </div>
  );
}

export default MainPage;
